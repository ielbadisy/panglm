% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{tidy.panglm}
\alias{tidy.panglm}
\title{Tidy a panglm fit into a data frame of coefficients}
\usage{
\method{tidy}{panglm}(x, conf.int = FALSE, conf.level = 0.95, ...)
}
\arguments{
\item{x}{a `"panglm"` fit}

\item{conf.int}{if `TRUE`, add `conf.low`/`conf.high` columns}

\item{conf.level}{confidence level for the interval}

\item{...}{unused}
}
\value{
a `data.frame` with one row per coefficient
}
\description{
Implements the `generics::tidy()` generic so `panglm` results work with
`broom`/`modelsummary` table-building workflows.
}
\examples{
data(copd)
fit <- panglm(fev1 ~ treatment + age + smoker + crp, data = copd,
              index = c("id", "visit"), model = "pooling", family = "gaussian")
if (requireNamespace("generics", quietly = TRUE)) {
  generics::tidy(fit, conf.int = TRUE)
}
}
